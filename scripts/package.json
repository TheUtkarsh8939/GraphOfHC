{
  "name": "graph-of-hc",
  "version": "1.0.0",
  "description": "Generate embeddings for Hack Club projects",
  "main": "generateEmbeddings.ts",
  "scripts": {
    "generate-embeddings": "ts-node generateEmbeddings.ts",
    "build-graph": "ts-node buildGraph.ts",
    "load-neo4j": "ts-node neo4jLoader.ts",
    "build": "tsc",
    "start": "node dist/generateEmbeddings.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "neo4j-driver": "^5.16.0"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  }
}
